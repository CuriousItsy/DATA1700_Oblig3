CREATE TABLE Ticket (
                        id INT AUTO_INCREMENT PRIMARY KEY,
                        film VARCHAR(255),
                        number INT,
                        firstName VARCHAR(255),
                        lastName VARCHAR(255),
                        phone VARCHAR(15),
                        email VARCHAR(255)
);